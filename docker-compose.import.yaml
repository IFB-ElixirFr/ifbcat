services:
  db:
    volumes:
      - .:/code # for dev purpose only !!!

  web:
    restart:
      on-failure
    volumes:
      - /var/ifbcat-importdata:/code/import_data # for import purpose only !!!
